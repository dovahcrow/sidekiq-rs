(function() {var implementors = {};
implementors["sidekiq"] = ["impl <a class="trait" href="https://docs.serde.rs/serde/de/trait.Deserialize.html" title="trait serde::de::Deserialize">Deserialize</a> for <a class="struct" href="sidekiq/struct.Job.html" title="struct sidekiq::Job">Job</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
